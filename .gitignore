# Dependencies
node_modules/

# Environment
.env
.env.local
.env.*.local

# Build output
dist/
build/
coverage/
*.tsbuildinfo

# Logs
*.log
logs/
logs-test/

# Temporary files
*.tmp
.temp/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Runtime temp files
_bitable_temp_*.json

# Legacy handoff inputs (GPT and Trae now both read local files directly)
GPT to Trae.txt
*handoff*.txt

# Phase 2F evaluation artifacts (regenerated by `npm run evaluate`)
artifacts/evaluation/

# lark-cli local auth artifacts (QR codes for human scan, never commit)
lark-config-qr*.png
lark-user-auth-qr*.png

# Phase 3+ run artifacts (Gate D reports, smoke test outputs)
artifacts/feishu-gate-d/
artifacts/feishu-smoke/

# Runtime products (Workstream B/C/D/E) — never commit
# data/: audit log JSONL (FileAuditRepository) + other runtime state
data/
# eng.traineddata: Tesseract WASM language data downloaded at runtime
eng.traineddata
# uploads/: screenshot upload directory (config.screenshotUploadDir default)
uploads/

